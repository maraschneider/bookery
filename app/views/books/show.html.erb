<div class="container">
  <% if policy(@book).destroy? %>
  <%= link_to "Delete book", @book, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-secondary my-3" %>
<% end %>
  <div class="d-flex justify-content-between">
    <%= render 'books/book_info' %>
    <%= render 'books/author_info' %>
  </div>
  <div class="d-flex justify-content-between">
    <%= render 'shared/review_card' %>

    <div class="bg-white p-3 my-3" style="width: 400px">
      <h3>Availability</h3>
      <div>TODO: Calender to be implemented ;-)</div>
    </div>
  </div>
  <%= render'books/new_request' %>
</div>
